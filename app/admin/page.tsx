'use client';import{useEffect,useState}from'react';import{useRouter}from'next/navigation';import{supabase}from'../../lib/supabase';export default function Admin(){const r=useRouter();const[msg,setMsg]=useState('Checking access…');useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){r.replace('/');return}const{data:p}=await supabase.from('profiles').select('role').eq('id',user.id).single();if(p?.role!=='admin'){r.replace('/');return}setMsg('Admin console')})()},[r]);return <main style={{maxWidth:1100,margin:'50px auto',padding:24}}><div className="panel"><h1>GoKeke Admin</h1><p>{msg}</p><p>This console is private and is not linked from the public frontend.</p></div></main>}
