'use client';
import {FormEvent,useEffect,useState} from 'react';import{useRouter}from'next/navigation';import{supabase}from'../../lib/supabase';
export default function Passenger(){const r=useRouter();const[s,setS]=useState<any>(null);const[email,setEmail]=useState('');const[pw,setPw]=useState('');const[name,setName]=useState('');const[mode,setMode]=useState<'login'|'signup'>('login');const[msg,setMsg]=useState('');useEffect(()=>{supabase.auth.getSession().then(({data})=>setS(data.session))},[]);async function go(e:FormEvent){e.preventDefault();setMsg('');if(mode==='login'){const{x,error}=await supabase.auth.signInWithPassword({email,password:pw});if(error)setMsg(error.message);else{setS(x.session);r.push('/ride')}}else{const{x,error}=await supabase.auth.signUp({email,password:pw,options:{data:{full_name:name,role:'passenger'}}});if(error)setMsg(error.message);else if(x.user){await supabase.from('profiles').upsert({id:x.user.id,full_name:name,role:'passenger'});setMsg('Account created. Check email confirmation if enabled.')}}}if(s)return <main style={{maxWidth:760,margin:'50px auto',padding:24}}><h1>GoKeke Passenger</h1><p>Book an Okada or Keke ride.</p><a className="btn lime" href="/ride">Continue to booking</a></main>;return <main style={{maxWidth:520,margin:'70px auto',padding:24}}><div className="panel"><h1>Passenger app</h1><p>{mode==='login'?'Sign in to book a ride':'Create your passenger account'}</p><form onSubmit={go}>{mode==='signup'&&<input required placeholder="Full name" value={name} onChange={e=>setName(e.target.value)}/>}<input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input required minLength={8} type="password" placeholder="Password" value={pw} onChange={e=>setPw(e.target.value)}/><button className="btn lime">{mode==='login'?'Sign in':'Create account'}</button></form><button className="btn" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'Create account':'I already have an account'}</button>{msg&&<p>{msg}</p>}</div></main>}
