import{NextResponse}from'next/server';export async function POST(req:Request){const key=process.env.FLW_SECRET_KEY;if(!key)return NextResponse.json({error:'Flutterwave is not configured'},{status:503});try{const b=await req.json();const r=await fetch('https://api.flutterwave.com/v3/payments',{method:'POST',headers:{Authorization:'Bearer '+key,'Content-Type':'application/json'},body:JSON.stringify({tx_ref:b.tx_ref,amount:b.amount,currency:'NGN',redirect_url:b.redirect_url,customer:b.customer,customizations:{title:'GoKeke ride'}})});return NextResponse.json(await r.json(),{status:r.status})}catch{return NextResponse.json({error:'Payment initialization failed'},{status:500})}}